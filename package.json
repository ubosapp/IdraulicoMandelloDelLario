{
  "name": "idraulico-mandello-vercel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp index.css dist/ && esbuild index.tsx --bundle --outfile=dist/index.js --jsx=automatic --minify"
  },
  "dependencies": {
    "@emailjs/browser": "^4.3.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.4",
    "typescript": "^5.4.5",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0"
  }
}