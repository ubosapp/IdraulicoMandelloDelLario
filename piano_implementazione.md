# Goal Description

Update the Idraulico Mandello Del Lario website to adopt the professional "Flat Design" design system generated by the UI/UX Pro Max skill. This will improve the conversion rate, trust, and overall aesthetic of the website by using a refined color palette, elegant typography, and modern UX patterns without emojis.

## User Review Required

Please review these proposed visual and structural changes. I will proceed with executing them once approved.

## Proposed Changes

### Configuration & Assets
#### [MODIFY] [index.html](file:///C:/Users/marco/OneDrive/Documents/Antigravity/IdraulicoMandelloDelLario/index.html)
- Change the Google Fonts import to `EB Garamond` (Headings) and `Lato` (Body).
- Ensure no emojis or generic font fallbacks are hardcoded.

### Styling (CSS)
#### [MODIFY] [index.css](file:///C:/Users/marco/OneDrive/Documents/Antigravity/IdraulicoMandelloDelLario/index.css)
- Update CSS variables with the new palette: 
  - `--color-primary: #1E40AF`
  - `--color-secondary: #3B82F6`
  - `--color-cta: #F97316`
  - `--color-background: #EFF6FF`
  - `--color-text: #1E3A8A`
- Update `--font-heading` to `EB Garamond` and `--font-body` to `Lato`.
- Update button styles (`.btn`) to use `#F97316` and flat design hover states.
- Clean up shadows to match the new flat design guidelines (only subtle drop-shadows where necessary).
- Ensure all hover animations and transitions use a smooth 150-300ms easing without scaling that breaks layout.

### Components & Content (React)
#### [MODIFY] [index.tsx](file:///C:/Users/marco/OneDrive/Documents/Antigravity/IdraulicoMandelloDelLario/index.tsx)
- Replace all emojis (used in the `services` array, zones list `📍`, and contact section `📞`) with modern inline SVG icons (from Lucide or Heroicons style) as per the anti-pattern guidelines.
- Update component classes if necessary to ensure buttons and interactive elements use proper styling.
- Verify `cursor-pointer` (or standard `cursor: pointer` in CSS) is applied on all clickables.

## Verification Plan

### Automated Tests
- Build and serve the app locally (`npm run dev` or a local static server depending on setup) to ensure components render without React errors.

### Manual Verification
1. I will check the rendering of the new SVG icons in place of emojis.
2. I will verify that the new Google Fonts load correctly and apply to headings and body text.
3. I will test hover states on buttons and links to ensure no layout shift occurs and transitions are smooth.
4. I will verify the contrast of text on buttons and backgrounds.
